# 构建只需要 Dockerfile、entrypoint.sh 和可选 patch 脚本；其余一律不进 build context，
# 避免运行数据、SQLite、证书、.env 和密钥备份被打包发送给 Docker daemon。
.git
data
caddy
authelia
.env
**/*.bak
README.md
TODO.md
LICENSE
deploy.sh
docker-compose.yml
